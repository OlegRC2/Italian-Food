import Parallax from './lib/parallax';              // подключение библиотеки параллакса

var scene = document.getElementById('scene');       // активация библиотеки параллакса
var parallaxInstance = new Parallax(scene);         // взято из документации

